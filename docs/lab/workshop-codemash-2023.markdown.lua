local ran_ok, error = pcall(function() local ran_ok, kpse = pcall(require, "kpse") if ran_ok then kpse.set_program_name("luatex") end local lfs = require("lfs") local cacheDir = "./_markdown_workshop-codemash-2023" if not lfs.isdir(cacheDir) then assert(lfs.mkdir(cacheDir)) end local md = require("markdown") local convert = md.new({cacheDir = "./_markdown_workshop-codemash-2023", frozenCacheFileName = "./_markdown_workshop-codemash-2023/frozenCache.tex", frozenCacheCounter = 0, hashEnumerators = true, smartEllipses = true, } ) local file = assert(io.open("code/cloudbotyaml.md", "r"), [[could not open file "code/cloudbotyaml.md" for reading]]) local input = assert(file:read("*a")) assert(file:close()) print(convert(input:gsub("\r\n?", "\n") .. "\n")) end) if not ran_ok then local file = io.open("./workshop-codemash-2023.markdown.err", "w") if file then file:write(error .. "\n") file:close() end print('\\markdownError{An error was encountered while executing Lua code}{For further clues, examine the file "./workshop-codemash-2023.markdown.err"}') end
